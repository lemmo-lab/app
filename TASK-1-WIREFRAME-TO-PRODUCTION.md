# تسک ۱: تبدیل وایرفریم به طرح و کامپوننت‌های نهایی دیزاین سیستم
## Task 1: Wireframe to Production Design System Implementation

> **وضعیت تسک:** آماده اجرا توسط توسعه‌دهنده (Ready for Developer Execution)  
> **مخزن اجرایی:** `/home/behroz/Documents/Git/lemu/app`  
> **مسئول اجرا:** توسعه‌دهنده فرانت‌اند (Frontend Developer / Coding Agent)  
> **پیش‌نیاز:** اتمام وایرفریم‌های ساختاری در فاز ۲  

---

### 🛑 قانون بنیادین این تسک: تفکیک منبع مقادیر واقعی از قواعد مصرف
> **معماری دوگانه‌ی منبع حقیقت (The Dual-SSOT Principle):**  
> ۱. **منبع یگانه حقیقت برای مقادیر واقعی (Values SSOT):**  
> پکیج اختصاصی **`@lemmo-lab/tokens`** (مستقر در مسیر `/home/behroz/Documents/Git/lemu/tokens` که به عنوان وابستگی در `app/package.json` نصب شده است)، **تنها مرجع رسمی مقادیر عددی، کدهای هگز رنگ‌ها، گرادیانت‌ها، ابعاد، رادیوس‌ها و متغیرهای `--lemmo-*`** است. تمام استایل‌ها باید از متغیرهای CSS و خروجی‌های این پکیج تغذیه شوند.  
> ۲. **منبع یگانه حقیقت برای قواعد و نحوه مصرف (Consumption & Judgment SSOT):**  
> مستندات دیزاین سیستم در پوشه `docs/design-system/`، منطق تصمیم‌گیری، چرایی، روابط معنایی و چگونگی به‌کارگیری توکن‌ها را تعیین می‌کنند.  
> 
> 🚫 **قانون اکید منع مقادیر هاردکدشده:** هیچ عدد پیکسلی، رنگ هگز، رادیوس یا مقدار حدسی خارج از متغیرهای پکیج `@lemmo-lab/tokens` نباید در کد نوشته شود. در صورت مشاهده حتی یک مقدار دستی خارج از پکیج، تسک بلافاصله توسط تیم ناظر **مردود** خواهد شد.

---

## ۱. فهرست اسناد و پکیج‌های مرجع الزامی (Sources of Truth)
پیش از نوشتن هر خط کد، توسعه‌دهنده موظف است پکیج توکن‌ها و اسناد زیر را مطالعه کرده و مستقیماً در توسعه به کار گیرد:

| ردیف | شناسه مرجع | نام منبع و مسیر | وظیفه و دامنه کاربرد در توسعه |
| :---: | :--- | :--- | :--- |
| **۰** | **`PKG-TOKENS`** | [**`@lemmo-lab/tokens`**](../../tokens/) (نصب در `app`) | **منبع انحصاری و یگانه مقادیر واقعی توکن‌ها** (رنگ‌ها، گرادیانت‌ها، فواصل، رادیوس‌ها و CSS Variables) |
| ۱ | `DOC-DS-001` | [`docs/design-system/STYLEGUIDE.md`](../docs/design-system/STYLEGUIDE.md) | راهنمای اجرایی استایل‌گاید، نحوه مصرف توکن‌ها و اصول بنیادین طراحی |
| ۲ | `DOC-DS-002` | [`docs/design-system/color.md`](../docs/design-system/color.md) | پالت رنگی معنایی، کنتراست WCAG AA، رنگ برند لیمو و رنگ‌های سطوح |
| ۳ | `DOC-DS-003` | [`docs/design-system/typography.md`](../docs/design-system/typography.md) | فونت‌های ۴ گانه (Morabba, IRANSansX, Oddval, Satoshi)، مقیاس متن و leading |
| ۴ | `DOC-DS-004` | [`docs/design-system/iconography.md`](../docs/design-system/iconography.md) | پکیج انحصاری Synthline، پله اندازه ۱۲ تا ۲۸ پیکسل و ضخامت خط ۱.۵ |
| ۵ | `DOC-DS-005` | [`docs/design-system/interaction-and-layout.md`](../docs/design-system/interaction-and-layout.md) | جهت‌بندی LTR/RTL، ویژگی‌های Logical CSS و سلسله‌مراتب اکشن‌ها |
| ۶ | `DOC-DS-007` | [`docs/design-system/design-decision-principles.md`](../docs/design-system/design-decision-principles.md) | **زاویه ۱:** درخت تصمیم فاصله‌گذاری، گرید ۴/۸، نسبت‌های تایپوگرافی و موشن |
| ۷ | `DOC-DS-008` | [`docs/design-system/container-relationship-and-inheritance.md`](../docs/design-system/container-relationship-and-inheritance.md) | **زاویه ۲:** فرمول ریاضی رادیوس هم‌مرکز و وراثت متغیرهای کانتینر |
| ۸ | `DOC-DS-009` | [`docs/design-system/responsive-decision-principles.md`](../docs/design-system/responsive-decision-principles.md) | **زاویه ۳:** چیدمان الگوریتمی، گرید پازلی، تطبیق تصاویر و الگوی Priority+ |
| ۹ | `DOC-DS-010` | [`docs/design-system/assistive-technology-and-microstates.md`](../docs/design-system/assistive-technology-and-microstates.md) | **زاویه ۴:** فناوری‌های کمکی، WCAG 1.4.13 محتوای شناور، اسکلتون و خطاهای فرم |
| ۱۰ | `DOC-DS-011` | [`docs/design-system/agent-operable-interfaces.md`](../docs/design-system/agent-operable-interfaces.md) | **زاویه ۵:** رابط دوستدار عامل، درخت دسترس‌پذیری، سه‌گانه Name/Role/Value و سلکتورها |
| ۱۱ | `DOC-ARCH-001`| [`app/AGENTS.md`](./AGENTS.md) | قوانین معماری، مرزهای وابستگی، دروازه `@/sdk` و الزام ۱۰۰٪ زبان انگلیسی در کد |
| ۱۲ | `DOC-FE-001` | [`app/BLUEPRINT.md`](./BLUEPRINT.md) | مشخصات فنی Next.js App Router، ساختار فولدرها و کتابخانه‌ها |

---

## ۲. گام‌های اجرایی توسعه‌دهنده (Implementation Steps)

### گام ۰: ممیزی و کشف تضاد (Stop Protocol)
- تمامی اسناد بالا را تطبیق دهید. اگر حتی یک تناقض یا مورد مبهم میان اسناد یافتید، دست نگه دارید، هیچ کدی ننویسید و گزارش تضاد را طبق دستورالعمل `docs/RULE.md` به مدیر تیم ارسال فرمایید.

### گام ۱: استخراج و ساخت پریمیتیوهای پایه (`src/shared/ui/primitives/`)
- پیاده‌سازی کامپوننت‌های پایه (`Button`, `Input`, `Textarea`, `Badge`, `Avatar`, `Tooltip`, `Modal`) بر اساس قراردادهای مصوب در `docs/design-system/primitives.md`.
- عدم ایجاد Props استایل مستقیم؛ تمامی استایل‌ها از طریق ماژول‌های CSS و کلاس‌های معنایی متصل به توکن‌های `--lemmo-*`.
- ارث‌بری الزامی از المان‌های بومی HTML با `forwardRef` و پشتیبانی از پترن `asChild` (الگوی Radix Slot).
- پیاده‌سازی تمام حالت‌های تعاملی پنج‌گانه (Default, Hover, Active, Focus-visible, Disabled, Loading با `aria-busy`).

### گام ۲: تبدیل پوسته استودیو (Studio Shell Transformation)
- جایگزینی بلوک‌های خنثی وایرفریم نوار کناری (`StudioSidebar.tsx`) با کامپوننت‌های نهایی دیزاین سیستم.
- اعمال ترنزیشن‌های استاندارد عرض سایدبار بر اساس بودجه موشن در `DOC-DS-007`.
- رعایت دقیق فرمول رادیوس هم‌مرکز برای تمامی دکمه‌ها و کانتینرهای درونی سایدبار طبق `DOC-DS-008`.
- پیاده‌سازی پاپ‌اور پروفایل با رعایت استانداردهای WCAG 1.4.13 (Dismissible با Escape، Hoverable و Persistent) طبق `DOC-DS-010`.
- حفظ کامل حالت هدر و دراور موبایل (۴۸px هدر + ۲۵۶px دراور اسلایدی بدون نوار پایین).

### گام ۳: تبدیل سطوح کاری و ویوها (`src/app/(workspace)/`)
- جایگزینی وایرفریم‌های سطح چت و کانواس با گرید الگوریتمی پازلی (`repeat(auto-fit, minmax(...))`) طبق `DOC-DS-009`.
- عدم استفاده از Breakpoint دستی مگر در نقاط شکست قطعی چیدمان.
- اتصال ورودی‌ها و فرم‌ها به برچسب‌های دسترس‌پذیر، `autocomplete` استاندارد، و وضعیت‌های `aria-invalid` و `role="alert"` طبق `DOC-DS-010` و `DOC-DS-011`.
- درج نشانه‌های معنایی ماشین‌خوان برای هدایت عامل‌های هوش مصنوعی (`data-action`, `data-component`, `aria-label`).

---

## ۳. چک‌لیست خودارزیابی نهایی توسعه‌دهنده (Developer Self-Checklist)
توسعه‌دهنده باید پیش از ارسال خروجی برای تیم ناظر، تک‌تک موارد زیر را بررسی کرده و اطمینان حاصل کند:

### الف) توکن‌ها، رنگ‌ها و فاصله‌گذاری
- [ ] تمامی متغیرهای مصرفی مستقیماً از پکیج نصب‌شده‌ی `@lemmo-lab/tokens` (شامل متغیرهای `--lemmo-color-*`، `--lemmo-space-*`، `--lemmo-radius-*`، `--lemmo-gradient-*`) تامین شده و هیچ مقدار هگز یا عددی هاردکدشده‌ای خارج از این پکیج وجود ندارد.
- [ ] تمامی فاصله‌ها (Padding و Margin) مضرب پله‌های ۴ و ۸ پیکسلی توکن‌های `space-*` پکیج توکن‌ها هستند.
- [ ] قانون `Internal ≤ External` در تمامی کارت‌ها و کانتینرها رعایت شده است.
- [ ] تمامی گوشه‌ها فرمول رادیوس هم‌مرکز ($\text{radius}_{\text{inner}} = \max(0, \text{radius}_{\text{outer}} - (\text{padding} + \text{border}))$) را پاس می‌کنند.
- [ ] کنتراست تمامی متون آزمون WCAG AA (حداقل ۴.۵:۱) را پاس می‌کند.

### ب) تایپوگرافی، زبان و دوزبانگی
- [ ] فونت‌ها از طریق ۴ استک استاندارد (Morabba, IRANSansX, Oddval, Satoshi) بارگذاری شده‌اند.
- [ ] هیچ کلمه‌ای برای جلب توجه دستی رنگی نشده و ضوابط تایپوگرافی `DOC-DS-003` رعایت شده است.
- [ ] برای تمامی ویژگی‌های فاصله‌ای و موقعیتی از Logical CSS (`margin-inline`, `padding-inline`, `inset-inline`) استفاده شده و هیچ `left` یا `right` فیزیکی وجود ندارد.
- [ ] سوییچ بین RTL و LTR بدون کوچک‌ترین اعوجاج یا شکست صفحه کار می‌کند.

### ج) آیکون‌ها و برند
- [ ] تمامی آیکون‌ها بدون استثنا از پکیج `synthline/react` با `strokeWidth={1.5}` و `color="currentColor"` هستند.
- [ ] اندازه آیکون‌ها دقیقاً در پله مجاز (۱۲، ۱۶، ۲۰، ۲۴، ۲۸) قرار دارد.
- [ ] نشان ترایاد ۳ نقطه‌ای لوگوی لیمو هندسه و ابعاد رسمی `DOC-BRAND-001` را حفظ کرده است.

### د) ریسپانسیو و چیدمان الگوریتمی
- [ ] چیدمان ستون‌ها با CSS Grid و فرمول پازلی خودکار انجام شده و با تغییر عرض بدون media query می‌شکند.
- [ ] تصاویر دارای `aspect-ratio` ثابت و بدون Layout Shift هستند.
- [ ] کاروسل‌ها فاقد پخش خودکار (Auto-play) بوده و برای اطلاعات حیاتی استفاده نشده‌اند.
- [ ] دکمه بازگشت در صفحات فرعی دارای رفتار Up، تاچ‌تارگت حداقل ۴۴×۴۴px و آینه‌سازی در RTL است.

### ه) دسترس‌پذیری، ریزحالت‌ها و عامل هوش مصنوعی
- [ ] محتوای شناور (Tooltip، منو) با کلید Escape بسته می‌شود، قابل‌هاور است و محو خودکار ندارد.
- [ ] دکمه‌های فقط-آیکون دارای `aria-label` یکتا و گویا هستند.
- [ ] تمام تصاویر یا `alt=""` دارند یا متن توصیفی کارکردی.
- [ ] عملیات ناهمگام با `aria-busy` و `aria-live` به درخت دسترس‌پذیری گزارش می‌شوند.
- [ ] فرم‌ها به `aria-invalid`، `aria-describedby` و `role="alert"` متصل هستند.
- [ ] المان‌های تعاملی دارای Role و Name معنادار در درخت دسترس‌پذیری Playwright هستند.

### و) کدنویسی و معماری مهندسی
- [ ] تمامی کامنت‌ها، نام متغیرها و داکیومنت‌های درون کد ۱۰۰٪ به زبان انگلیسی هستند.
- [ ] هیچ داده یا فایلی کلمه `mock` را خارج از مسیر ایزوله `@/sdk` وارد نکرده است.
- [ ] تست بیلد پروژه (`pnpm run build`) بدون هیچ خطای تایپ یا لینت پاس می‌شود (Exit Code 0).

---

> ⚠️ **تحویل تسک:**  
> پس از تیک خوردن ۱۰۰٪ موارد فوق و اطمینان از صحت بیلد، توسعه‌دهنده گزارش اتمام تسک ۱ را صادر کرده و پروژه را به مرحله بعد ارجاع می‌دهد. تسک ۲ (ممیزی و اعتبارسنجی تیم) آغاز خواهد شد.
